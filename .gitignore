# Data URL config — contains Gist URLs, gitignored so live URLs stay private
config.js

# NOTE: billing_data.json and dashboard_data.json ARE tracked on 'data' branch
# They're only gitignored on 'main' branch to avoid committing local dev data
*.jsonl

# Editor / OS
.DS_Store
.env
*.env
